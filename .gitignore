# Dependencies
node_modules/

# Environment / secrets — never commit
.env
.env.*
!.env.example

# Expo / tooling caches
.expo/

# OS / editor
.DS_Store
*.log
*.swp

# Flutter/Dart build artifacts (apps/mobile/.gitignore also covers these)
**/.dart_tool/
**/build/
**/.flutter-plugins
**/.flutter-plugins-dependencies

# Legacy React Native app (superseded by apps/mobile Flutter)
/mobile/
