import 'package:flutter/material.dart';

/// Non-web: in-app iframe embedding isn't available; the guide sheet falls back
/// to a "Watch form video" button. Returns null so the caller can branch.
Widget? buildInAppVideo(String youtubeId) => null;
