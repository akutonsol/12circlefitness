// Placeholder smoke test — the default Flutter counter test was removed since
// this app does not use the counter template. All feature tests live in
// test/unit/ and test/widget/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — no tests needed in root widget_test.dart', () {
    expect(true, isTrue);
  });
}
